namespace Cloudtoid.Json.Schema
{
    public sealed partial class JsonSchemaWriter
    {
        protected internal override void VisitString(JsonSchemaString constraint) => base.VisitString(constraint);
    }
}
